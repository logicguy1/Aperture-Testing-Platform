const config = {
  baseurl: `http://${window.location.hostname}/api`,
  days: 3
};

export default config;

/*
 * baseurl: The url the API is located at
 * gantt: The gantt driagram licence key, get one at https://gstc.neuronet.io/free-key/
 *
 *
 *
 *
 */

